<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="KnolageTests.Pages.ArticlePage"
             Title="{Binding Title}"
             BackgroundColor="{StaticResource BackgroundColor}">

    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="12">

            <!-- Thumbnail (hidden when not provided) -->
            <Image x:Name="ThumbnailImage"
                   Source="{Binding ThumbnailPath}"
                   Aspect="AspectFit"
                   HeightRequest="180"
                   HorizontalOptions="Center"
                   IsVisible="False" />

            <!-- Title and subtitle -->
            <Label Text="{Binding Title}"
                   FontSize="26"
                   FontAttributes="Bold"
                   LineBreakMode="WordWrap" />

            <Label Text="{Binding Subtitle}"
                   FontSize="16"
                   TextColor="Gray"
                   LineBreakMode="WordWrap" />

            <!-- Tags -->
            <ScrollView Orientation="Horizontal" HorizontalScrollBarVisibility="Never">
                <HorizontalStackLayout x:Name="TagsContainer" Spacing="8" Padding="0,4,0,0" />
            </ScrollView>

            <!-- Blocks rendered dynamically -->
            <VerticalStackLayout x:Name="BlocksContainer" Spacing="10" />

            <!-- Tests frame (kept from original page) -->
            <ScrollView>
                <VerticalStackLayout Padding="16" Spacing="12" WidthRequest="400">
                    <!--<Label x:Name="TitleLabel" Text="{Binding Title}" FontSize="22" FontAttributes="Bold" LineBreakMode="WordWrap"/>
                    <Label x:Name="SubtitleLabel" Text="{Binding Subtitle}" FontSize="16" TextColor="Gray" LineBreakMode="WordWrap"/>
                    <FlexLayout x:Name="TagsLayout" Wrap="Wrap"/>
                    <Image x:Name="Thumbnail" Aspect="AspectFit" HeightRequest="160" HorizontalOptions="Center"/>
                    <VerticalStackLayout x:Name="BlocksStack" Spacing="10"/>-->

                    <!-- Tests section: populated from code-behind -->
                    <VerticalStackLayout x:Name="TestsContainer" Spacing="8" Margin="0,8,0,0">
                        <!-- dynamically filled with Buttons or a Label -->
                    </VerticalStackLayout>

                </VerticalStackLayout>
            </ScrollView>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>